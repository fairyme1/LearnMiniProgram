<!--pages/home/home.wxml-->
<text>pages/home/home.wxml</text>
<view class="outer" bindtap="handleout">
外层的box-view
  <view class="inner" bindtap="handleinner">内层的box-view</view>
</view>

<view class="container">
   <block wx:for="{{titles}}" wx:key="{{index}}">
      <view class="items" bindtap="handleclick" data-index="{{index}}" data-item="{{item}}">
        {{item}}
      </view>
  </block>
</view>
